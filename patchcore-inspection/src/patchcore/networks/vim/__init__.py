"""Vendored Vision Mamba (Vim) backbone from github.com/hustvl/Vim (vim/).

Only models_mamba.py + rope.py are vendored; the forked bidirectional mamba_ssm and
causal_conv1d CUDA kernels must be installed in the dedicated `vim` conda env.
"""
